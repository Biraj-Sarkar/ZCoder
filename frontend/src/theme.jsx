import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // custom blue
    },
    secondary: {
      main: "#dc004e", // custom pink
    },
  },
});

export default theme;
